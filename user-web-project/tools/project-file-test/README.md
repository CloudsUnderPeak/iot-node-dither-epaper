# Project file test

Runs a browser round trip for the `.dither.png` PNG container and verifies automatic routing for project and ordinary PNG files.

```bash
python3 tools/project-file-test/run.py
```
